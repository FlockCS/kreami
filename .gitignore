# macOS
.DS_Store
*.pem

# Dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts
.metro-health-check*

# Native build output (managed workflow regenerates these)
/ios
/android
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# TypeScript
*.tsbuildinfo

# Logs
npm-debug.*
yarn-debug.*
yarn-error.*

# Environment & secrets
# Catch-all: the earlier list missed .env.production, .env.staging and friends,
# which would have been committed silently. Only the placeholder file is kept.
.env*
!.env.example

# Supabase CLI local state
supabase/.branches/
supabase/.temp/

# Editors
.idea/

# Generated design canvases (~2 MB each of packaged editor code).
# Rebuilt from the .dc.html artboards + canvas.json via the /design skill.
design/**/kreami-*.html
